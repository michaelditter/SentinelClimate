node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key

# development detritus — pasted transcripts leaked identifiers once already
attached_assets/
.vercel
server-dist

# runtime state (watchlist persistence)
.data/
